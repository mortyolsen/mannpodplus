// theme.js
// All of the app's colors and spacing live here, in ONE place.
// If we ever want to change a color across the whole app,
// we change it here once — not in fifty different files.

export const colors = {
  background: "#14110F", // deep, warm near-black (dark wood at night)
  card: "#1F1B18", // a slightly lighter warm panel for the cards
  cardBorder: "#2C2722", // a soft, barely-there edge on the cards
  textPrimary: "#E8E2D6", // soft warm off-white (never harsh pure white)
  textMuted: "#9A8F80", // quiet, warm grey for secondary text
  accent: "#C8A06A", // warm amber, like late-night lamp light
};

export const spacing = {
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
};